# Fixture: report

Synthetic incident postmortem for a manager audience. Demonstrates:
`summary` (executive summary), `timeline`, `finding`, `comparison`
(options), `decision`, `actions`.

```bash
pnpm forma render fixtures/report/forma.spec.json --out fixtures/report/output
pnpm forma preview fixtures/report/output
```
