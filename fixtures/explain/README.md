# Fixture: explain

Synthetic mobile SDK initialization flow. Demonstrates: `cover`,
`key-points`, `flow`, `annotated-code` (with line highlights + note-rail
annotations), `finding` with `confidence: inferred`, `glossary`,
`source-note`.

```bash
pnpm forma render fixtures/explain/forma.spec.json --out fixtures/explain/output
pnpm forma preview fixtures/explain/output
```
